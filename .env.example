# ============================================================
# OpenWA — Environment Variables
# Copy to .env for local dev. On Render: set in Dashboard.
# NEVER commit a filled .env to git.
# ============================================================

# REQUIRED — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
API_MASTER_KEY=CHANGE_ME_STRONG_RANDOM_SECRET_MIN_32_CHARS

# Server
NODE_ENV=production
PORT=2785

# Engine: baileys (no Chrome) or whatsapp-web.js (needs Chromium)
ENGINE_TYPE=baileys
BAILEYS_AUTH_DIR=./data/baileys
SESSION_DATA_PATH=./data/sessions

# Database — SQLite (default) or postgres
DATABASE_TYPE=sqlite
DATABASE_NAME=./data/openwa.sqlite
# DATABASE_TYPE=postgres
# DATABASE_HOST=your-pg.render.com
# DATABASE_PORT=5432
# DATABASE_USERNAME=openwa
# DATABASE_PASSWORD=strong_password
# DATABASE_NAME=openwa
# DATABASE_SSL=true

# Storage
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./data/media
# STORAGE_TYPE=s3
# S3_BUCKET=openwa-media
# S3_REGION=auto
# S3_ACCESS_KEY_ID=your-key
# S3_SECRET_ACCESS_KEY=your-secret
# S3_ENDPOINT=https://account.r2.cloudflarestorage.com

# Queue / Redis (optional)
QUEUE_ENABLED=false
REDIS_ENABLED=false
# QUEUE_ENABLED=true
# REDIS_HOST=your-redis.upstash.io
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

# Security
CORS_ORIGINS=
TRUSTED_PROXIES=10.0.0.0/8

# Features
ENABLE_SWAGGER=true
LOG_LEVEL=info
PLUGINS_DIR=./data/plugins

# Webhooks
WEBHOOK_TIMEOUT=10000
WEBHOOK_MAX_RETRIES=3
WEBHOOK_RETRY_DELAY=5000
